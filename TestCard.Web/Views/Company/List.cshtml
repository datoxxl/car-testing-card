@{
    ViewBag.Title = "List";
}
    @{ Html.RenderPartial("_ActionBar", new ActionBar
       {
           EntityObject = TestCard.Domain.Objects.Company,
           Items = new List<ActionButton>
           {
               new ActionButton { 
                   Type = ActionButton.ActionTypes.Add, 
                   Text = GeneralResource.Add,
                   Url = Url.Action("Add", "Company")
               }
           }
       }); }
@{ Html.RenderPartial("_AjaxList", new AjaxListModel { ActionName = "Company" }); }
