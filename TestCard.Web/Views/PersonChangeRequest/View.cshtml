@model TestCard.Web.Models.PersonModel

<div class="x-form-container">
    <span class="header">@GeneralResource.User</span>
    <div class="top-actions">
        @Html.ActionLink(GeneralResource.Back, "List")
    </div>
    <div class="form-inner">
        @Html.DisplayForModel()
        <div class="x-actions">
            @using (Html.BeginForm("ProcessRequest", "UserChangeRequest", new { @id = Model.PersonChangeRequestID, status = (int)TestCard.Domain.ConfirmStatuses.Approved }, FormMethod.Post))
            {
                <input type="submit" value="@GeneralResource.Approve" />
            }
            @using (Html.BeginForm("ProcessRequest", "UserChangeRequest", new { @id = Model.PersonChangeRequestID, status = (int)TestCard.Domain.ConfirmStatuses.Rejected }, FormMethod.Post))
            {
                <input type="submit" value="@GeneralResource.Reject" />
            }
            @Html.ActionLink(GeneralResource.Cancel, "List")
        </div>
    </div>
</div>
