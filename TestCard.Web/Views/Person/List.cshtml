@{
    ViewBag.Title = "List";
}
<p>
    @{ Html.RenderPartial("_ActionBar", new ActionBar
       {
           EntityObject = TestCard.Domain.Objects.Person,
           Items = new List<ActionButton>
           {
               new ActionButton { 
                   Type = ActionButton.ActionTypes.Add, 
                   Text = GeneralResource.Add,
                   Url = Url.Action("Add", "Person")
               }
            }
       }); }
</p>
@{ Html.RenderPartial("_AjaxList", new AjaxListModel { ActionName = "Person" }); }
