@model TestCard.Web.Models.TestingCardChangeRequestModel

@{
    ViewBag.Title = "View";
}
<div class="x-form-container">
    <span class="header">@GeneralResource.TestingCardChangeRequest</span>
    <div class="top-actions">
        @Html.ActionLink(GeneralResource.BackToList, "List")
    </div>
    <div class="form-inner">
        @Html.DisplayForModel()
        @{ Html.RenderPartial("_RequestActions", new TestCard.Web.Models.RequestActionsModel
            {
                Action = "ProcessRequest",
                Controller = "TestingCardChangeRequest",
                RequestID = Model.TestingCardChangeRequestID.Value
            }); }
    </div>
</div>
